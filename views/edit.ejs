<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4Q6Gf2aSP4eDXB8Miphtr37CMZZQ5oXLH2yaXMJ2w8e2ZtHTl7GptT4jmndRuHDT" crossorigin="anonymous">
</head>
<body>

<h2 class="mb-4">Edit Application</h2>

<form action="/updateForm" method="POST" enctype="multipart/form-data" class="card p-4 shadow-sm">
    <input type="hidden" name="id" value="<%- applicant.id %>">

    <div class="mb-3">
        <label class="form-label">Name</label>
        <input type="text" name="name" value="<%- applicant.full_name %>" class="form-control" required />
    </div>

    <div class="mb-3">
        <label class="form-label">Email</label>
        <input type="email" name="email" value="<%- applicant.email %>" class="form-control" required />
    </div>

    <div class="mb-3">
        <label class="form-label fw-bold">Phone Number</label>
        <input type="number" name="pnumber" value="<%- applicant.phone %>" class="form-control" required />
    </div>

    <div class="mb-3">
        <label for="position" class="form-label">Position Applied For</label>
        <select class="form-select" id="position" name="position" required>
            <option disabled value="">Choose...</option>
            <option value="Frontend" <%- applicant.position_applied === 'Frontend' ? 'selected' : '' %>>Frontend</option>
            <option value="Backend" <%- applicant.position_applied === 'Backend' ? 'selected' : '' %>>Backend</option>
            <option value="Full Stack" <%- applicant.position_applied === 'Full Stack' ? 'selected' : '' %>>Full Stack</option>
        </select>
    </div>

    <div class="mb-3">
        <label class="form-label fw-bold">Upload Resume</label>
        <input type="file" name="resume" class="form-control" accept=".pdf,.jpg,.jpeg,.png" />
        <% if (applicant.resume) { %>
            <small class="text-muted">Current Resume: <%= applicant.resume %></small>
        <% } %>
    </div>

    <button type="submit" class="btn btn-primary w-25 m-auto my-3">Update Application</button>
    <a href="/allApplications" class="btn btn-secondary w-25 m-auto">Cancel</a>
</form>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js" integrity="sha384-j1CDi7MgGQ12Z7Qab0qlWQ/Qqz24Gc6BM0thvEMVjHnfYGF0rmFCozFSxQBxwHKO" crossorigin="anonymous"></script>
</body>
</html>